:root{
  --ground:#E8E5DB; --panel:#FAF8F2; --sunk:#F1EEE5;
  --ink:#17150F; --muted:#6E6857; --rule:#CBC5B4; --hair:#DFDACC;
  --accent:#B4321F; --done:#4C5A3F;
  --mono:"Liberation Mono","DejaVu Sans Mono",ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;
  --serif:Georgia,"Liberation Serif","Times New Roman",serif;
}
@media (prefers-color-scheme:dark){
  :root:not([data-theme="light"]){
    --ground:#14130E; --panel:#1E1C15; --sunk:#191710;
    --ink:#EFECE1; --muted:#968F7D; --rule:#332F26; --hair:#262319;
    --accent:#D2603F; --done:#8B9673;
  }
}
:root[data-theme="dark"]{
  --ground:#14130E; --panel:#1E1C15; --sunk:#191710;
  --ink:#EFECE1; --muted:#968F7D; --rule:#332F26; --hair:#262319;
  --accent:#D2603F; --done:#8B9673;
}
*{box-sizing:border-box}
body{margin:0;background:var(--ground);color:var(--ink);font-family:var(--serif);
  font-size:16px;line-height:1.6;-webkit-font-smoothing:antialiased}
h1,h2,h3{margin:0;line-height:1.2;text-wrap:balance}
p{margin:0}
button{font:inherit;color:inherit;cursor:pointer}
a{color:var(--accent)}
:focus-visible{outline:2px solid var(--accent);outline-offset:2px}
.wrap{max-width:860px;margin:0 auto;padding-inline:22px}

/* ---- sticky progress ---- */
.bar{position:sticky;top:0;z-index:10;background:var(--ground);
  border-bottom:1px solid var(--rule)}
.barin{display:flex;align-items:center;gap:16px;padding:13px 0;flex-wrap:wrap}
.barin h1{font-family:var(--mono);font-size:13px;letter-spacing:.2em;text-transform:uppercase}
.track{flex:1;min-width:120px;height:7px;background:var(--sunk);
  border:1px solid var(--rule);overflow:hidden}
.fill{height:100%;width:0;background:var(--done);transition:width .28s ease}
.tally{font-family:var(--mono);font-size:12px;color:var(--muted);
  font-variant-numeric:tabular-nums;white-space:nowrap}
.reset{background:none;border:0;font-family:var(--mono);font-size:11px;
  letter-spacing:.1em;text-transform:uppercase;color:var(--muted);
  text-decoration:underline;text-underline-offset:3px;padding:0}
.reset:hover{color:var(--accent)}
@media (prefers-reduced-motion:reduce){.fill{transition:none}}

/* ---- intro ---- */
.intro{padding:34px 0 8px}
.eyebrow{font-family:var(--mono);font-size:11px;letter-spacing:.24em;
  text-transform:uppercase;color:var(--accent)}
.intro h2{margin-top:14px;font-size:clamp(26px,4.6vw,38px)}
.intro p{margin-top:14px;color:var(--muted);max-width:62ch}

/* ---- steps ---- */
.steps{display:flex;flex-direction:column;gap:18px;padding:26px 0 90px}
.step{background:var(--panel);border:1px solid var(--rule);
  display:grid;grid-template-columns:auto 1fr;gap:0 18px;padding:20px 22px}
.step.done{background:var(--sunk);border-color:var(--hair)}
.step.done .head h3,.step.done .body{opacity:.5}
.tick{grid-row:1/3;width:34px;height:34px;border:2px solid var(--rule);
  background:transparent;border-radius:50%;display:flex;align-items:center;
  justify-content:center;font-family:var(--mono);font-size:13px;font-weight:700;
  color:var(--muted);flex:0 0 auto;padding:0}
.tick:hover{border-color:var(--accent);color:var(--accent)}
.step.done .tick{background:var(--done);border-color:var(--done);color:var(--panel)}
.head{display:flex;align-items:baseline;justify-content:space-between;gap:14px;
  flex-wrap:wrap}
.head h3{font-size:19px}
.mins{font-family:var(--mono);font-size:11px;color:var(--muted);white-space:nowrap}
.body{margin-top:9px;display:flex;flex-direction:column;gap:12px}
.body>p{color:var(--muted);font-size:15px;max-width:62ch}
.go{display:inline-block;align-self:flex-start;background:var(--accent);color:#FBFAF5;
  padding:10px 18px;font-family:var(--mono);font-size:12px;letter-spacing:.12em;
  text-transform:uppercase;text-decoration:none;border:0}
.go:hover{filter:brightness(1.12)}

/* ---- copyable field ---- */
.field{border:1px solid var(--rule);background:var(--ground)}
.flabel{display:flex;align-items:center;justify-content:space-between;gap:10px;
  padding:8px 11px;border-bottom:1px solid var(--rule);background:var(--sunk)}
.flabel span{font-family:var(--mono);font-size:10.5px;letter-spacing:.16em;
  text-transform:uppercase;color:var(--muted)}
.copy{background:var(--ink);color:var(--ground);border:0;padding:6px 13px;
  font-family:var(--mono);font-size:10.5px;letter-spacing:.12em;text-transform:uppercase}
.copy:hover{background:var(--accent);color:#FBFAF5}
.copy.ok{background:var(--done);color:#FBFAF5}
.fval{padding:12px;font-size:15px;line-height:1.55;white-space:pre-wrap;
  max-height:230px;overflow:auto}
.fval.code{font-family:var(--mono);font-size:13px;white-space:pre;overflow-x:auto}

/* ---- tags ---- */
.tags{display:flex;flex-wrap:wrap;gap:7px;padding:12px}
.tag{display:flex;align-items:baseline;gap:7px;border:1px solid var(--rule);
  background:var(--panel);padding:5px 9px;font-family:var(--mono);font-size:12px}
.tag b{font-weight:400}
.tag i{font-style:normal;font-size:10px;color:var(--muted);
  font-variant-numeric:tabular-nums}
.tag.tight i{color:var(--accent)}

/* ---- file rows ---- */
.files{display:flex;flex-direction:column;gap:0;border:1px solid var(--rule)}
.file{display:flex;align-items:baseline;gap:12px;padding:9px 12px;
  border-bottom:1px solid var(--hair);font-family:var(--mono);font-size:12.5px}
.file:last-child{border-bottom:0}
.file b{font-weight:400;flex:1;word-break:break-all}
.file i{font-style:normal;color:var(--muted);font-size:11px;white-space:nowrap}
.file em{font-style:normal;color:var(--accent);font-size:10.5px;letter-spacing:.1em;
  text-transform:uppercase;white-space:nowrap}

.note{border-left:3px solid var(--accent);padding:11px 14px;background:var(--sunk);
  font-size:14.5px;color:var(--muted)}
.note b{color:var(--ink);font-weight:400}

.after{border-top:2px solid var(--ink);padding:26px 0 70px;margin-top:12px}
.after h2{font-family:var(--mono);font-size:13px;letter-spacing:.2em;
  text-transform:uppercase;margin-bottom:16px}
.after ul{margin:0;padding-left:20px;color:var(--muted);max-width:64ch}
.after li{margin-bottom:9px}
.after b{color:var(--ink);font-weight:400}